# 退货商品差异人工待办

2026-09-24 修正模块1 `ERP_RETURN_ITEM_MISMATCH` 待办：原模板只保留笼统原因和客户累计应收，却隐藏了型号、颜色、数量，业务员无法直接定位差异。

新正文在原因后列出两组商品：

> 【售后工作台 订单:示例订单】 店铺：示例店；原因：ERP退货单型号颜色数量不一致；本单应退：MODEL-A/铬×1件；ERP退货单商品：MODEL-B/铬×1件；请核对退货型号、颜色和数量差异，再处理平账。明细见售后工作台。

“本单应退”取该售后记录的完整 SKU、颜色和申请数量；SKU 中的 `#颜色` 在颜色字段缺失时拆出展示。ERP 商品取已保存的退货核验行，不把单据描述称为仓库实物验货结论。多行完整列出，不截断规格，不仅凭型号相同分配其他订单的数量。缺少明细时明确标为待核实，不能编造应退或实收。

这类型号/颜色/数量差异提醒不再展示“客户累计应收”，原值仍保存在审计载荷。客户总账余额不代表本单商品正确或已平账。其他待办类型及退款、退货匹配规则不变。

生成新任务及刷新原有可发送任务时写入商品明细，发送入口沿用统一模板。旧任务缺少应退明细时显示“待核实”；已发送记录保留原文、原凭证和原防重标识，不为了改文字删除或重复发消息。当前修复不认领退货、不改 ERP 单据、不退款或补开退款单。

运行入口和周期、发布开关、业务员归属、数据库结构均不变，无新增环境变量。正式候选仅在当前后台版本上叠加 `services/manual_todo_text.py`、`workflows/module1_manual_todo.py` 的本次改动，不夹带开发分支未上线的业务规则。隔离候选测试通过后，使用现有停止标志等待完整后台周期自然退出，再原子切换后台源码指针并恢复守护；不强杀请求。指针和源码核验记录保存在 Git 忽略的 `.runtime/audits/return-mismatch-text-20260924/`，回退只恢复原源码指针，不回滚业务账本。

验证覆盖型号差异、数量差异、多行规格、旧任务明细缺失、原标识查重、重复格式化及原始载荷保留，不向业务员发送测试消息。
