# 2026-09-11 安全版本准备记录

本轮完成失败任务只读核验、102边界修正和候选版本准备。没有执行真实退款、ERP写入、群消息发送、生产数据库迁移或后台切换。测试通过不代表实单验收，也不代表可以无人值守运行。

## 失败任务核验

对原8个FAILED资金动作逐一读取拼多多最新售后详情，并核对售后号、平台订单号及申请金额：2个读到明确退款完成；其余6个未获得明确成功事实，不能据此认定未退款或直接重试。随后对2个退款完成任务只读查询ERP退货匹配，均为退款流水待核验，尚不能确认财务闭环。

本轮没有重置FAILED、重新入队或回填闭环。平台已退款的任务不得再次调用退款；应继续核验唯一ERP退款事实及财务条件。另6个保留原状态与证据，仅允许只读回查或人工核验。真实订单、客户和逐笔证据仅存放在Git忽略的 `.runtime/audits/safety-release-prep-20260911/`。

## 本次修正

- `auto_uncollected.validate_auto_shipping`：将距发货不超过24小时改为真实发货日期必须等于北京时间当天；继续拒绝未来发货、发货晚于证据及快递公司变化。
- `auto_uncollected.require_auto_execution`：资金执行处于北京时间09:00（含）至21:00（不含），与客服配置取交集。配置全天不能扩大固定窗口。
- 历史复查测试统一服务及仓库时钟，补24小时到期前、到期点、到期后测试；未修改生产每日复查算法。
- 普通在途仍按已确认规则：已发货全额仅退款，排除派件/派送、签收等阻断，完成身份、金额、物流、关联关系、人工锁等校验，群通知成功后可先退款，不等待拦截成功或入库。通知成功不代表拦截成功。

## 候选版本与运行版本

候选版本从本分支已提交源码建立独立发布副本；不混入工作区其他未提交开发改动。候选包含前期资金防重、包裹发送防重、退款前复核、ERP质检保护，以及已提交的待办文案、企微前台重试和历史每日复查。

运行版本仍是 `.runtime/releases/pdd-history-20260910`，数据库仍为 `20260909_0025`。候选所需的 `20260910_0026`、`20260910_0027` 尚未应用到生产。当前运行版本不应被描述为已经具备候选版本的全部保护。

## 迁移和上线前置条件

0026创建资金防重账本。旧平台RUNNING/FAILED/SUCCEEDED及非取消的旧ERP相关资金任务先保留UNKNOWN；不能因为新账本为空而重做。0027创建包裹发送账本，历史成功为LEGACY_SENT，仅防止重发；历史不明确结果为UNKNOWN。

本轮尝试在同MySQL服务器创建独立合成数据测试库，账号返回1044（无创建库权限），演练在创建测试库阶段停止。生产库前后均为0025，新账本表数量均为0。迁移离线SQL可以生成，但不代替真实MySQL迁移验收。未修改生产账号权限或改用生产库演练。

下一步需要具备建表及迁移权限的独立MySQL测试库。以合成数据覆盖：同售后重复平台任务合并为同一个资金键；旧ERP待执行任务隔离；取消任务不生成资金记录；同运单大小写空格归一；历史成功与未知混合时以UNKNOWN保护；空快递公司不形成凭证；Python幂等键与MySQL SHA2一致；跨连接竞争仅一次占用；任务删除或重启不能解除防重。

真实演练通过后，准备数据库、运行源码和版本指针备份，停止守护并等待后台周期自然结束，确认所有写入进程停止，再迁移0026/0027并切换对应版本。禁止新旧worker并行执行。升级后先核验历史隔离和失败任务，再分模块观察。不得强杀可能正在发送资金请求的后台。

回退时须冻结外部写操作，并保留两张防重账本；旧代码不读取新账本，不能仅恢复旧版本并继续自动执行。迁移downgrade拒绝删除账本。

## 运行限制

- 102与双接口无轨迹仍是独立策略，用户要求的14项共同条件尚未全部统一，不能宣称完成整个审计。
- 天猫前5店自动入口仍因整包裹关系适配不完整而失败关闭，第6店禁止资金写操作。
- 缺独立仓库质检、多包裹或存在不明确实收分配的退货转人工；本地锁不代表外部仓库已经阻止出库。
- 企业微信发送键后的画面变化不能证明消息发送成功。无消息级回执时保留SendPressed并人工核验，不自动重发；未知发送记录可能阻断后续桌面任务。
- “人工已处理”与“确认已发群”的最终业务语义尚需明确，本轮未改变该语义。
- 完整复合售后主键迁移、跨平台关联适配、同步授权错误展示和安全回补仍有后续工作。

## 验证记录与复现

本轮工作区针对性测试127项、全量1067项全部通过，0失败、0错误、0跳过；修改的Python文件Ruff通过。独立提交快照 `0b5fe13` 的1064项测试也全部通过，0失败、0错误、0跳过，已验证导入路径来自候选副本。工作区多出的3项来自既有未提交测试，不混入候选。候选源码位于 `.runtime/releases/safety-20260911`；本节后续说明补充不改变该已测试代码提交。测试使用模拟资金接口，没有借测试执行真实退款。

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_auto_uncollected.py tests/test_pdd_history.py tests/test_audit_safety_regressions.py -q
.\.venv\Scripts\python.exe -m pytest tests -q --basetemp=.runtime/audits/safety-check-new-temp
.\.venv\Scripts\python.exe -m ruff check src/aftersales_workbench/workflows/auto_uncollected.py tests/test_auto_uncollected.py tests/test_pdd_history.py
.\.venv\Scripts\python.exe -m alembic upgrade 20260909_0025:head --sql
```

日期用例包括昨天22点到今天10点、23:59:59到次日00点、同日零点和未来时间；工作时间包括08:59:59、09:00:00、20:59:59、21:00:00；服务端按America/New_York传入时间仍按Asia/Shanghai判定。原90秒及重启保护用例继续执行。

对应说明已更新自动待揽收、安全修复和历史复查文档，并随代码提交GitHub。当前没有可调用的Notion连接器，未更新Notion页面。
