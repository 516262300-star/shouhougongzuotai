# 天猫模块1/2有限开放（2026-09-11）

用户授权“目前能开通的先开通”。本次仅发布独立单订单、单子单、单包裹核验及对应功能状态，不放开多订单多包裹自动分配，也不发布模块3。

## 范围与当前授权检查

- 第1店利德仕、第2店 leedis、第4店梵居匠、第5店固家恒：现配退款子账号可只读查询本店退款单，纳入有限范围。
- 第3店珂琪艺：当前子账号调用 `taobao.user.seller.get` 和 `taobao.refund.get` 均返回 `code=12 / subuser.has-no-permission`。这不等于证明退款写接口一定无权，但不足以确认当前授权可用，先从退款白名单排除；后续核对实际授权账号及更新凭证再验收。
- 第6店适家：无独立退款凭证，继续排除。
- 查询权限成功不是资金接口实际成功验收。本次不以任意历史订单试退款，不声称已完成真实退款或ERP闭环。

模块1要求成功发送凭证及最新物流闸门，支持现有在途、退回中、已退回分支；无轨迹/未揽收特殊退款不在此范围。模块2要求真实实收、独立仓库验货、完整SKU/颜色/数量与ERP原销售和退货记录一致。客户原销售包含其他订单、跨售后运单冲突、缺资料、人工锁、历史占用、批准快照变化、核验超时均阻断。ERP明细一致不冒充仓库质检。

## 发布方式

沿用 `scripts/module1-worker.ps1` 常驻周期及 `scripts/module1-autostart.ps1` 既有登录/守护恢复，未改变频率。输入为平台售后、物流与ERP只读原销售/退货证据；输出为既有任务审计与单次平台资金请求。幂等、UNKNOWN保护、人工锁、工作时间、订单水位不变，不批量重置历史失败任务。

运行副本：

- Worker：`.runtime/releases/tmall-limited-worker-20260911/src`，基于 `pdd-return-history-worker-20260911`，仅覆盖 `actions.py`、`package_orders.py`、新增 `tmall_single_parcel.py` 及单包裹配置字段。原拼多多、企微恢复、历史退货分类逻辑保留，不带入开发树快线程或模块3。
- Web：`.runtime/releases/tmall-limited-web-20260911/src`，基于 `monitor-focus-web-20260911`，仅更新功能状态和必要配置字段，前端构建不变。模块1/2显示“有限开启”，不显示整个平台全模块开启。
- `.env`：`TMALL_SINGLE_PARCEL_REFUND_ENABLED=true`；`TMALL_REFUND_ENABLED_SHOP_NUMBERS=[1,2,4,5]`；模块3保持关闭。通用配置默认仍为关闭，不把本机启用状态设成所有部署的默认值。
- 无数据库迁移、无新ERP采购、无ERP接口部署；天猫模块1退回后的自动补单仍未接入，模块3所需只读路由未部署，继续关闭。

## 验证与回退

发布前新增合成执行链测试，覆盖单次调用、金额变化、证据过期、执行权变化、仓检失败及第6店阻断；另外检查隔离运行副本的天猫、拼多多共享包裹、仓检和资金幂等回归。只读授权检查不进行资金请求。

原 `.env` 与两个发布指针保存在 Git 忽略的 `.runtime/audits/tmall-limited-rollout-20260911/`，不得上传。回退时先将单包裹开关改为 false，安全停止 worker 等待当前周期自然退出，再恢复旧 worker/Web 指针并启动；不要强杀在途资金请求，不覆盖后续其他配置更新，不删除幂等账本或重试 UNKNOWN 请求。

2026-09-11 15:25（Asia/Shanghai）已完成本机切换：旧 worker 安全退出后，新 worker PID 29976 从上述独立副本启动；Web 经控制台正常退出信号关闭后，由原守护脚本启动为 PID 39832。登录/周期守护已恢复，状态为 healthy；`/health/ready` 返回应用及数据库正常。

线上 `/api/v1/monitor/capabilities` 已验证：第1、2、4、5店模块1/2显示“有限开启”；第3、6店及全部天猫模块3显示“未开启”。生产前端索引 SHA256 保持 `07E71217EFAD5DF0533FCA29A6CBDF5C1A355575DECE53CC22FD332925068A40`。未做浏览器点击验收，不将接口验证写成浏览器验收。

隔离 worker 副本143项相关回归、隔离 Web 副本3项状态/API测试通过；前期新增合成路径等38项测试也通过（与前述用例有重叠，不累计宣称独立总数）。保留一项第三方弃用提示。首轮运行于15:24:34开始、15:26:51结束，天猫模块1、2退款执行阶段均 completed、无阶段错误；扫描及实际退款数均为0。尚无此次上线后的真实退款成功或ERP全闭环验收，不以周期运行成功冒充退款成功。

本次功能与发布说明提交 `79551a9`，已成功推送到 GitHub 分支 `codex/audit-safety-fixes`。发布指针记录该代码提交及各自原运行副本提交，方便追溯组合发布。

发布准备期间复制了非必要的前端开发依赖，已停止该复制进程；生产构建完整且哈希一致，不影响业务进程。未删除任何业务数据或备份。未发现可访问的 Notion 目标，本次同步 README 与仓库运行说明。
