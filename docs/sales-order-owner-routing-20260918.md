# 待办按发货销售订单归属分派（2026-09-18）

客户档案的归属业务员可能与具体销售订单不同。人工待办必须读取原发货销售单每笔订单的“归属业务员”，不能回退客户档案归属。普通模块1、2、3待办和同包裹待办均适用；待办发布开关、资金授权和同包裹人工锁保持原规则。

## 取值与发送

- ERP 网页查询先通过平台订单号定位唯一客户、验证客户ID，再读取发货销售单全部分页，精确匹配目标平台订单的商品行。客户ID用于定位，不用于确定收件人。只查询归属时允许客户历史中存在其他平台订单，但不放宽整包裹退款核验的跨平台保护。
- 同一订单多行须全部为同一非空业务员；归属缺失、冲突、漏页、重复页或读取失败时保留本地待核实。旧SQL适配器仅读取销售订单业务员；退货匹配不再用客户档案人员覆盖销售归属缓存。
- 发布待办前再次只读核验销售订单归属，覆盖旧排队任务中的错误收件人。读取失败不领取发送任务、不消耗发送次数，保存原因并冷却5分钟，由后续后台周期重查。待办列表仍可看到未发送状态及原因。
- 同包裹只对有效退款申请所对应的销售订单业务员分组；同一人合并一份，申请订单对应不同人员则每人一份。未申请退款订单用于说明业务阻断，不单独通知其业务员。
- 正文列出“您负责的退款申请订单”。同包裹使用客户、快递公司、运单、业务员和归属来源构成v2幂等键及独立外部标识；迁移旧任务时不沿用旧收件人的外部查重标识。发送前发现归属变化则重新分组，避免迁移走的订单又被合回原人。
- 只修改待发送任务；已发送历史记录保留接收人、正文、ERP待办ID和成功凭证。指定个案需要纠正接收人时，先只读复核当前申请及归属，备份后新增正确人员待办，并在旧记录追加关联说明，不能把原记录改成“早已发给正确人员”。

## 运行与维护

沿用正式机 `Leedis Aftersales Module1 Watchdog` 登录触发和每5分钟守护；现有worker循环负责同步和发布。没有新增定时任务、数据库迁移或配置项。依赖现有 `ERP_WEB_BASE_URL`、`ERP_WEB_USERNAME`、`ERP_WEB_PASSWORD` 只读销售查询及原ERP待办发布权限；不要将账号、订单或审计资料提交Git。

查不到业务员时先核对该订单是否已在ERP发货销售单、商品行归属是否完整，再查看任务 `owner_routing_status` / `owner_routing_retry_after` 和 `last_error`。更正ERP资料后下一轮到期核验会重新分派。禁止直接把客户档案人员填入队列绕过核验。

## 验证、发布和回退

测试使用内存数据库、模拟ERP及平台接口，覆盖档案与订单业务员不同、多业务员分别通知、同人合并、归属缺失/冲突、旧待办改派、发送前归属变化、查询期间取消、冷却与恢复、同包裹防重、发布开关和已发送凭证保留。真实订单采用只读销售记录及平台申请核验，不用退款或快递群消息进行测试。

发布基于正式机 `business-hold-worker-20260918` 完整代码复制候选目录，只合入本次差异，逐文件校验原文件与候选SHA-256。等待原后台周期自然结束，使用 `scripts/office_verified_backup.py` 备份全部业务表、运行配置和发送账本后，原子切换worker发布指针并启动原守护。Web发布指针不变，避免夹带开发工作区其他未完成变更。

回退前先关闭人工待办发布开关，再停机并切回保存的前一worker指针。旧版按客户档案分派，不应在回退后继续发布待办。保留新旧任务及所有发送凭证，不恢复旧数据库、删除幂等记录或重发历史任务。

个案证据保存在Git忽略的 `.runtime/audits/sales-order-owner-20260918/`。对应运行说明已同步到仓库；未定位到需同步的Notion页面。GitHub推送失败时，交接必须注明本地提交号和远端未更新状态。

候选版本相关回归测试 **204项通过**，Ruff通过；仅有既有Starlette/httpx弃用警告。指定个案只读复核发现整票四笔订单已在平台退款成功，因此本次只修正本地归属缓存并给原通知追加归属审计，不补发已过期的“部分订单未申请退款”提醒，不改动平台资金状态。

## 正式机验收

- 已启用 `sales-owner-worker-20260918`，代码提交 `53a4e64`。上一版指针和162个候选文件的哈希已归档，正式目录再次逐文件校验通过。
- 备份位于 `D:\LDSAftersales\backups\sales-owner-20260918-155954`，验证21张业务表、9个运行文件，数据库版本 `20260914_0028`。
- 北京时间16:00:10至16:04:10完成首个完整周期，`ok=true`，各阶段无失败。销售归属同步扫描20笔，11笔已匹配、9笔资料未能完整读取，保留待重查，未回退客户档案人员。本轮没有待发布任务，因此没有为验收制造真实待办。
- 原守护健康，worker处于交互会话1，维护停止标记已撤销。企微待发、运行中、失败队列均为0，没有未确认发送阻塞。
- 指定两笔订单归属缓存已改为对应销售订单人员；原已发待办保留原收件人和ERP凭证，只追加归属更正及不补发原因。整票已退款事实来自平台只读回查；本次无新增资金请求、快递群消息或人工待办。
- 运行审计和本次个案证据已同时归档到正式机及开发机Git忽略目录。以上验收结果补充同步到仓库说明及正式机运行说明。

## 2026-09-21 零数量历史商品行修复

部分ERP原销售单保留数量为0的历史商品行。此前业务员查询复用了整包裹正数数量限制，即使目标订单另有正常正数商品行、全部归属明确，也会返回“归属暂不可读”，使人工待办停留在发送前，尝试次数为0。

只读归属查询现在显式启用限定目标订单的查询模式：允许同单零数量行，但至少须存在一条正数原销售商品行，所有行的销售关联ID、数量格式和业务员归属仍须完整一致。零数量行不删除，也不能忽略其归属冲突或空值；负数、非数值、非有限数量、全零订单继续待核验。客户身份、全部分页及稳定性验证继续保留。其他整包裹、退款、实收核验调用保持默认正数限制，归属查询结果不授予数量验收或资金资格。

仍沿用原后台周期和5分钟归属重查冷却，不新增计划任务或配置。恢复已有待办时，先核对原状态、尝试次数与远端幂等标识，再由原发布器核验归属、发布开关、领取原任务、查重及发送回查；不能删除重建任务、改写已发送历史或把验收通知与后续退款申诉通知混作同一事项。个案证据仅存 `.runtime/audits/manual-todo-owner-20260921/`。

新增回归覆盖正数与零行同存、全零、零行归属冲突/缺失、无效销售ID、负数与非数值，以及包裹数量保护未放宽。发布基于当前正式Worker仅更新原销售读取与业务员解析两个文件；Web、独立揽收提醒和资金开关保持现状。

北京时间10:13:55已启用 `manual-todo-owner-worker-20260921`，代码提交 `33d0621` 已推送GitHub。本地与正式候选各89项相关测试通过，Ruff通过；两笔真实订单只读预演均准确匹配原销售业务员。原后台自然结束后完成验证备份 `D:\LDSAftersales\backups\manual-todo-owner-20260921-101353`（24张表、10个文件），切换后由原守护恢复到桌面Session 1，其他两个发布指针哈希一致。

在同一维护窗口，按用户指定的两条原任务限定恢复：再次核实平台退款成功，保留原幂等键，清除本次失败查询冷却，再由原发布器完整执行归属、开关、任务领取、远端查重和发送回查。两条均于10:14确认成功，各一次提交，正式HTTP接口与ERP待办编号一致；既有已发送验收通知保持原样，没有删除重建或重复退款。一个待发申诉沿用的“已冻结平台退款”历史措辞按最新成功退款事实更正，原文保存在本地审计。此修复不替代退货异常本身的人工核实或平台申诉处理。

本次说明同时同步正式机和Notion工作台页面；个案订单、人员和ERP待办编号仅保留在运行审计和原业务系统，不提交到Git或Notion。
