# 德邦消息已发送但回执核验暂停

## 原因与修复

2026-09-13 09:20 的德邦通知已实际发送，但发送后的全文核验失败，账本保留为 `SendPressed`，后续通知队列因此暂停。9 月 14 日核对实际群画面，原完整消息在正确群中可见、输入框为空、发送状态正常。

原识别器为定位布局将整个高分屏截图缩小到 1400 像素宽，又把裁出的消息交给 OCR 放大识别，丢失了文字笔画。本次故障画面中“【售后快递拦截】”被识别为“【售后快递栏截】”，该行置信度约 0.917，低于原有 0.95 要求，因此完整消息不被接受。

`wecom_receipt.py` 现在仍用归一化图像定位气泡，但将气泡坐标映射回原始截图，再直接裁剪原始文字交给 OCR。同一故障画面的标题正确识别、置信度约 0.997，完整运单和正文均通过。未降低置信度阈值，未模糊匹配文字，仍要求准确群名、完整正文、唯一自身发送气泡、空输入框和明确状态连续通过。

## 验证与上线

- 80 项相关发送、群名/草稿/回执、前台与恢复测试通过。新增高分屏测试确认 OCR 得到原始像素，既有错误群名、错误运单、错误正文、双气泡与异常状态拒绝检查保留。
- 基于正式机当前版本建立 `wecom-native-receipt-worker-20260914`，逐文件核对只有 `wecom_receipt.py` 改变，保留其他平台与资金逻辑。安全等待原后台退出，完成数据库、账本、配置和代码备份，再切换 Worker 指针；原 Web、数据库与业务配置未替换。
- 在正式机实际桌面执行原有连续回执校验，原德邦消息全部通过。结合用户明确反馈与实际画面，保存证据及原账本后，通过既有 `confirm_sent` 和数据库关联任务核销流程确认原通知，没有按发送键，没有重新发送，没有触发资金请求。
- 阻塞已解除，关联同包裹任务随现有去重逻辑完成；恢复时队列由 18 待发、3 运行中变为 11 待发、0 运行中。任务数量不等于实际新发送消息数量，不能将关联任务核销计作补发。

原始截图、OCR 输出、完整计划、原账本、确认结果和旧发布指针均位于受保护且 Git 忽略的 `.runtime/audits/deppon-receipt-20260914`；不提交个案运单或客户信息。后续通知继续走原有物流核验、包裹去重及发送保护，不批量清空账本或重置未知结果。
