# 天猫六店平台退款启用（2026-09-16）

用户明确要求将天猫尚未开启退款的店铺加入现有退款流程。本次把正式机退款白名单从第1、2、4、5店扩大至第1–6店，即增加珂琪艺和适家，不扩大ERP写入或复杂合包自动退款。

## 运行环境与授权

正式环境在办公机 `D:\LDSAftersales\app`，开发机所有原停用开关保持不变。开发机数据库停用是迁移后的预期状态，不代表正式机故障。本次上线前正式机 `/health/ready` 返回应用与数据库正常。

用户提供的新主账号与子账号 session/refresh_token 均从开发机安全配置合并至正式机候选 `.env`，共同AppKey/AppSecret一致性已经校验；仅更新24个凭证字段和 `TMALL_REFUND_ENABLED_SHOP_NUMBERS=[1,2,3,4,5,6]`，其余正式开关和配置保持原值，不用开发机整份 `.env` 覆盖正式配置。六店主、子账号各自的身份、退款列表和一笔近期详情只读查询均在正式机候选版本通过。refresh_token 仅保存，未新增自动续期，也未调用刷新接口。

正式机原有天猫同步、平台写总开关、模块1/2执行、单订单单包裹核验及所需ERP只读开关已开启，无须恢复其他平台或ERP写开关。凭证、完整配置及个案数据均保存在受限且Git忽略的审计目录，不上传GitHub。

## 代码范围与安全条件

- 天猫店铺加载器允许1–6店的显式白名单；主/子凭证缺失、重复、未知店号仍阻止加载。
- 平台客户端和执行器允许白名单第6店进入平台退款；保留天猫主账号审核、子账号同意退款顺序及每次写请求只发送一次。
- 永久资金账本仅将第6店的 `PLATFORM_REFUND` 纳入能力范围；ERP退款能力仍限定原先范围，UNKNOWN和历史已执行任务防重复保护不变。
- 模块1仍需通知成功、实时物流和独立单订单单子单单包裹核验；无轨迹特殊风险退款未因此加入天猫。模块2仍需真实实收、独立仓检、ERP原销售/退货明细及平台最新申请核验。多订单、多包裹、部分退款、人工锁、证据不全或发生变化均按现有规则阻断。
- 能力矩阵六店平台退款显示已开启，模块1/2显示“有限开启”。适家的ERP认领、补单及模块3不随平台退款权限自动扩大。
- 配置开启不等于平台资金接口已实单验收。上线不以历史订单发起测试退款，不清理任务或资金账本来重试；后台只在既有规则满足时正常处理。

## 发布与验证

本地165项回归通过（含第三、第六店模块1/2单次执行、金额/证据变化、白名单缺失、仓检不通过、未知第7店、重复资金请求以及ERP范围隔离），保留一项第三方弃用提示。

正式机候选版本从当前 Worker `wecom-title-worker-20260915`、Web `refund-pending-display-web-20260912` 分别复制，只按原文件SHA-256核对后打入本次8个文件改动。两者不是同一代码基线；不以开发树文件覆盖正式机后续企微、拼多多和展示修复。候选版本命名为 `tmall-six-shop-worker-20260916`、`tmall-six-shop-web-20260916`。前端构建、数据库schema与自动化频率不变。

切换顺序：持有现有守护周期锁，创建工作进程安全停止标记并等待整棵进程树自然退出；在实际桌面会话通过专用一次性任务向已核实的Web独立控制台发送正常退出信号，不强杀资金任务；执行既有 `office_verified_backup.py` 校验数据库与运行文件备份，再替换候选配置和两个发布指针，清理本次维护标记/一次性任务，通过原交互会话守护任务恢复。失败不继续扩大权限；不初始化数据库、不迁移开发数据。

回退须先把第3、6店从退款白名单排除并安全停机，选择保留的兼容旧发布指针；新凭证可保留，不能整份回写旧配置覆盖后续变更。原永久资金和发送账本必须保留，结果不明只读核验，不自动重发。审计目录为正式机 `.runtime/audits/tmall-six-shop-20260916/`。无可定位的Notion说明页，本次同步仓库部署说明。

## 正式机生效验收

- 2026-09-16 10:34切换完成，代码提交 `76126a5`。切换前备份 `D:\LDSAftersales\backups\tmall-six-shop-20260916-103438` 已校验21张表、9项运行文件，schema为 `20260914_0028`。
- Web 首次恢复因新目录缺少配套前端产物而被启动保护阻止；随后从原Web发布目录复制未修改的前端，逐文件SHA-256比对一致，再由原守护恢复。未使用开发前端构建或替换业务数据。部署准备流程已补充前端产物与哈希检查。
- 10:36:53线上健康接口返回应用和数据库均正常，功能接口确认六店 `refund_permission=enabled`、模块1/2均为“有限开启”；适家模块1ERP仍为“核账已开·认领补单未接入”，模块3未开启。此为实时接口验证，未声称浏览器点击验收。
- Worker PID 5292在正式桌面Session 1运行，10:34:48–10:36:15首个完整周期 `ok=true`。周期成功不等于本次新增店铺已完成真实退款验收，未另行发起测试资金请求。
- 实际 `.env` 与经校验的候选配置哈希一致，正式主/子账号新凭证均已加载；本次维护停止标记已清理，一次性Web退出任务已移除，开发机仍停用。
