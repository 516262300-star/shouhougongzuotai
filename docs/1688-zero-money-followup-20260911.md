# 2026-09-11 1688零金额售后只读同步修复

## 发现与口径

上线后的7笔1688隔离项均因退款金额为0而被拒绝。逐笔只读详情与5笔原订单对照显示：6笔属于带`refundFlowType=zero_money`及退换货标记的退货流程，1笔属于`cbu_reshipping`补寄流程。申请货款、申请运费、实际退货款和实际退运费4个字段均明确为0。

6笔的接口原类型仍为退货流程，因此保留`RETURN_AND_REFUND`，不凭内部replace标记擅自改成普通换货；补寄记录为`RESEND`。金额0不代表缺失金额已被补全，更不代表已退款或ERP已闭环。原异常标识和诊断保留，正常入库成功后才标为已恢复，不使用忽略/删除异常来消除提醒。

## 实现

- 只有流程名、请求类型、退货/仅退款布尔字段以及具体非资金标记全部一致，才允许零金额导入；缺标记、未知类型仍隔离。
- 4个金额字段须完整、有限且原始数值严格为0。缺字段、非数值、正数、负数或小数金额冲突均阻断，不能把小金额四舍五入成0。
- 原订单身份、商品行唯一性、明细关联及正整数数量必须明确。查询失败、空订单、缺商品、重复行或不完整数量不能按正常记录导入。
- 1688零金额退货和补寄的资金状态为`NOT_APPLICABLE`，实际退款金额及退款完成时间为空。`refundsuccess`只保留平台流程状态，不产生资金成功或ERP闭环事实。
- 已有退款成功或实际退款金额的记录不得被零金额增量覆盖，冲突转核验。
- 1688继续只读同步、记录和归因，不新建平台退款、ERP补单、锁单或快递群任务，不扩展任何平台资金白名单。

## 验证与部署边界

针对性47项通过，覆盖身份/金额冲突、缺字段、正数/非法/小数金额、原订单查询超时、重复导入、无自动资金任务、不可覆盖既有资金事实。7笔真实只读样本在本地规范化预演均通过；未把真实客户和订单样本提交GitHub。

初次独立候选全量回归为1087通过、21失败；失败集中于旧未揽收测试使用固定样本日期、平台预检却读取真实时钟，跨日后触发24小时保护。测试夹具已统一冻结预检时钟，新增未来1秒、刚发货、24小时前1秒、恰好24小时及超时1秒边界，相关29项通过。未修改生产时钟或放宽24小时保护。独立候选最终全量1113项通过，0失败、0错误、0跳过；变更文件Ruff通过。

```powershell
.\.venv\Scripts\python.exe -m pytest tests/test_1688_non_financial.py tests/test_marketplace_mappers.py tests/test_refund_financial.py tests/test_marketplace_sync.py -q --basetemp=.runtime/audits/1688-check-new-temp
```

本轮从实际运行的`6baec59`建立独立发布分支，仅叠加本修复及测试说明，不带入其他已提交或未提交的个案流程开发。无数据库结构迁移。部署时备份当前运行指针、配置、账本与受影响记录，先停守护、等后台自然退出，再切换候选并恢复。数据回补仍逐笔重新读取平台；异常恢复和记录入库同事务提交，不重置资金任务或历史发送账本。

## 实际部署结果（北京时间2026-09-11）

- 10:18完成逐笔最新只读复核和事务回补：创建7笔记录、恢复7笔原隔离异常，0平台写入、0资金操作、0自动任务。原错误内容、重查次数和异常记录保留，未忽略或删除异常。
- 10:19切换后台到独立候选`5c60f2e`，路径`.runtime/releases/safety-1688-20260911/src`，数据库仍为`20260910_0027`。守护随后恢复，进程健康检查正常。
- 停稳后完整数据库备份为25,756,157字节；配置、发送账本、运行指针、旧周期日志及数据库明细一并备份，SHA-256清单在本地证据目录。2540条历史资金防重记录、133条包裹发送凭证及历史失败任务在回补和切换时均未改变；`.env`逐字节不变。
- 10:19:04至10:20:16首周期：拼多多7店、天猫6店、其他平台4店同步完成；1688未处理隔离项为0。企业微信本周期无发送任务，原先待确认的新通知在既有账本与数据库中均已是Sent，任务成功，未重复发送。
- 首周期平台退款及ERP补单执行数为0。模块3另有1笔只读核验返回“ERP欠货表结构不完整，不能视为无欠货”，状态为`unavailable`，未按空结果放行或补单，因此该周期总体`ok=false`。这是后续需核查的ERP响应问题，不能用同步恢复替代整体验收。
- 10:23再次复核：7笔记录仍无自动任务，1688隔离项仍为0，全部历史资金记录及失败任务不变；第二周期各平台同步继续完成，模块3仍有查询失败，未执行资金操作。

验收复测须在独立候选工作树执行，使用新的工作区临时目录：

```powershell
$project = (Get-Location).Path
$previousPythonPath = $env:PYTHONPATH
Push-Location .runtime/releases/safety-1688-20260911
try {
  $env:PYTHONPATH = (Join-Path (Get-Location).Path 'src')
  & (Join-Path $project '.venv/Scripts/python.exe') -m pytest tests -q `
    --basetemp=(Join-Path $project '.runtime/audits/1688-acceptance-new-temp')
} finally {
  $env:PYTHONPATH = $previousPythonPath
  Pop-Location
}
```

恢复时保留现有数据库和永久账本，禁止恢复旧库覆盖已发生的资金/发送事实，禁止删除账本或重置历史FAILED/UNKNOWN任务。修复的是1688非资金同步隔离问题，不代表多包裹自动退款、ERP完整闭环或企业微信无人值守发送已验收。

工作台网页进程未重新发布；新的非资金状态展示代码须在后续网页服务正常更新后加载，本轮不宣称网页已完整更新。后台持久化资金状态优先保证准确。

企业微信后续新通知的SendPressed必须逐条人工核验，上一运单的确认不能复用到另一条通知。前台激活已能在后续周期恢复，但当前仍无可靠的消息级自动回执，不能靠窗口变化或按键成功自动确认Sent。

证据与数据备份仅存Git忽略的`.runtime/audits/post-upgrade-followup-20260911/`。该轮推送曾因GitHub连接重置或443端口连接失败未完成，后续ERP修复时连接恢复，本轮代码、说明及发布分支已补推到远端。无可调用Notion连接器，未更新Notion。

后续ERP欠货查询误报与恢复过程见[ERP空状态修复记录](erp-outstanding-empty-state-20260911.md)，保留本记录中各时间点的原始观察，不把后续恢复改写为当时已完成。
