# 企业微信输入前证据过期自动恢复

## 原因与修复

新正式机发生一次输入前暂停：同包裹核验约两秒完成，但准备输入时距证据起始时间已约212秒，超过原有80秒有效期。原订单快照一致、没有同包裹人工处理锁；输入前截图确认目标群正确、输入框为空、相同消息气泡为0，任务尝试次数为0。现有审计未覆盖全部窗口准备步骤，不能据此确定延迟来自哪一步。

原代码把证据过期和身份变化都抛为普通错误，留下无自动重试时间的 `PausedBeforePaste`，阻塞后续队列。本次仅把“任务仍待执行、订单快照一致、无同包裹人工锁、证据年龄大于80秒”区分为可重查的输入前过期。冷却60秒后由下一次正常通知周期重新取得完整平台和包裹证据，再核验群名、输入框及永久发送账本。

没有延长80秒有效期，也没有改写旧证据时间来放行。未来时间、缺失证据、订单身份或金额变化、取消任务继续拒绝输入。已进入 `PasteStarted` 或 `SendPressed` 的任务不能被该异常降级重发，仍沿用既有防重与回执处理。消息气泡成功即可确认发送，不依赖快递回复。

## 入口与现场恢复

入口仍为新正式机的 Module1 Worker 和 `Leedis Aftersales Module1 Watchdog`；不新增常驻任务或配置。到期重试由实际业务周期触发，并非保证60秒发出。

对本次旧格式暂停记录，先在发送锁内核对完整账本历史均未输入、任务仍待执行且尝试次数为0、原发送方案哈希未变、包裹无发送占用，以及原始证据与失败审计。只有全部满足才单独追加可重试暂停记录，保留旧历史；不按错误文本批量解锁，不使用“确认已发送”冒充成功。个案资料保存在 Git 忽略的 `.runtime/audits/wecom-repair-20260923/`。

发布基于新机当前正式 Worker，只替换 `notice_package_guard.py` 和 `desktop_sender.py`，保留人工待办、仓库验货、退款事务及宽屏企微修补。等待原周期自然退出、备份原发布指针与发送账本后切换，原交互桌面守护恢复运行。

## 验证

回归覆盖212秒超时但零输入、冷却到期前不恢复、恢复后重新完整核验且只发送一次、80秒边界、证据缺失/身份变化/取消/时钟异常不自动重试，以及已输入或已按发送键绝不降级重发。部署后的真实发送结果另行记录，离线测试不等同现场消息成功。

首版 `f42ee5e` 本地和新机候选各133项相关测试通过，18:58切换到新机并受控恢复原输入前暂停记录。19:01后台已自动重新取得包裹证据，但第一次群名识别耗时17.156秒，超过输入前12秒循环预算，仅获得一次正确群名确认，未输入正文并再次自动冷却。这是现场新增确认的第二处耗时缺口。

后续补丁将输入前只读群名核验预算增至45秒，与既有发送后核验预算一致；连续两次精确群名、空输入框、无相同消息和输入前80秒包裹证据要求保持不变。逐次识别耗时及总耗时写入原受保护审计，错误群、草稿、安全验证及结果不明仍不会放行。发布只在首版之上替换 `windows_wecom.py`，不覆盖同期其他模块。

第二版代码 `91504d7` 本地和新机候选各208项相关测试及Ruff通过，19:07已发布 `wecom-evidence-retry-worker-20260923-v2`。仅合并输入前等待函数的差异，保留正式基线其他代码。首条原暂停通知在19:06已由首版自动重新核验并发送，连续三次回执通过，数据库成功且实际输入尝试为1；没有人工补发或等待快递回复。

19:13资源采样另发现一条从18:05遗留的只读发布状态查询进程长期未退出，工作集约6.7GB、私有提交约20GB，形成明显内存压力；审计不能据此把每次延迟都归因于单一原因。19:14核实进程创建时间、完整只读命令及其哈希、无子进程后，仅结束该检查进程，新机可用内存恢复至约9.4GB。没有终止业务后台、数据库或企微。后续部署核验优先使用紧凑状态接口或Python有界读取日志；检查命令超时后须核对其远端检查进程是否退出，不能无上限重开相同查询。具体PID、命令和现场证据仅保存在受保护审计目录。

19:15第二条原积压通知在第二版下自动发送成功：输入前两次群名识别共3.5秒，发送后三次严格回执共6.547秒，输入框为空、唯一自身完整气泡、发送状态正常。两条原通知均为Sent，数据库成功、实际输入尝试各1次，无重复发送。19:16现场复核桌面阻塞为空，期间新入队的一条通知仍按正常每周期1条规则处理；不能把修复过程中新增队列当成原通知重发。没有改变退款权限，也没有为验证额外发送测试消息。
