# 项目工作规则

- 都用中文回答。
- 保护工作区既有改动，提交时只包含本次任务的文件或变更。
- 修改脚本、同步任务、自动化或定时任务代码后，如有对应 README、Notion、GitHub 或其他外部使用说明/备份，必须同步更新说明并提交推送；推送失败要报告本地提交号及远端未更新状态。

## 多订单、多包裹退货核验

用户已要求：遇到同一客户订单较多、分多个包裹退回、平台填写的退货单号可能交叉错配时，按客户本批全部相关订单和全部退货包裹合并核查。

执行前阅读 `docs/customer-multi-parcel-return-verification.md`。不得只查目标售后的快递单号，也不得仅凭同客户或型号相同就退款。通过原销售记录追溯平台订单，逐项核对完整型号、颜色、数量，建立唯一实收数量分配，排除已退款、已占用、重复售后及有争议记录。

“查一下/能否退款”仅授权只读核验；实际退款须有用户明确授权。授权后在明确范围内处理已核实且平台状态允许的订单，不反复索要同一授权，不扩展到未核实订单。资金操作前复核平台最新状态、申请金额及SKU，持久化幂等记录；资金请求不自动重试，结果不明时仅做只读回查。

本方法是核验与人工授权处理流程，不等于开启无人值守多包裹自动退款。不得伪造一对一收货单、仓库质检通过或ERP平账完成，不得为个案关闭全局防重复退款保护。个案订单号、客户资料及审计证据存放在 Git 忽略的 `.runtime/audits/`，不要提交到远端。
