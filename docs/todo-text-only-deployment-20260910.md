# 人工待办文案独立部署（2026-09-10）

## 范围

用户要求仅让新待办文案生效，不捆绑资金、消息防重迁移或其他业务改动。后台采用启动前的提交 `fd2efe5` 作为基线，单独加入文案模板、旧标识查重兼容和实际发送文字审计。开发工作区保留原状，Web 不切换版本。

六类待办去掉模块编号、重复主订单号、冗长商品明细，保留原因、处理要求及全部不重复的关联订单。新任务和旧排队任务发送前都格式化；已发送任务不修改、不重发。退款、企微、ERP 的开关、间隔、水位、凭据及历史人工锁不变。

## 后台版本固定

- 部署源码：`.runtime/releases/todo-text-20260910/src`，仅本地保存。
- 选择配置：`.runtime/module1-worker-release.json`，字段 `source_path` 为项目相对源码路径。
- `scripts/module1-worker.ps1 Start` 在子进程环境中设置 `PYTHONPATH`，启动前验证真实导入目录，之后恢复父进程环境；不修改系统环境、依赖安装、`.env` 或 Web。
- 目录必须在 `.runtime/releases/` 下并包含后台入口。配置损坏或目录缺失时拒绝启动，不能静默回退开发代码。没有选择配置时保持旧启动行为。
- 登录守护仍调用同一启动脚本，因此电脑重启后继续加载固定版本。

重建发布目录：将 `fd2efe5` 用 `git archive` 导出到新目录，先用 `git apply --check --unidiff-zero` 检查，再用 `git apply --unidiff-zero` 应用 `deployments/todo-text-only-20260910.patch`。补丁只含六个源码文件和对应测试，不含密钥、客户、数据库迁移或退款规则。发布前确认补丁检查及测试通过。

## 安全切换与回退

1. 保存当前选择配置（若存在）、后台日志和发布源码版本。
2. `module1-autostart.ps1 -Action StopWatch`，确认守护自然退出，避免停机中重新启动。
3. `module1-worker.ps1 -Action Stop`，等待当前周期和整个进程树退出；30秒未停并不等于失败，不强杀。
4. 写入已验证版本配置，再 `module1-worker.ps1 -Action Start`；检查进程、代码目录、周期日志及网页健康。
5. `module1-autostart.ps1 -Action StartWatch` 恢复守护。

回退同样先安全停机，将选择配置指向保留的兼容版本再启动。**当前开发分支含未部署的数据库迁移，不能通过删除选择配置直接回退到工作区。** 不清理发送账本，不改变任务状态来重发。

## 验证

独立发布目录全量离线回归：912项通过。启动版本选择测试：5项通过，覆盖未配置、合法目录、越界目录、入口缺失和损坏配置。测试不访问真实平台或发送待办。

16:33 已安全切换并恢复登录守护；启动前校验实际 Python 导入位于独立发布目录。16:34 首个完整周期结束，待办发布阶段完成且无错误，本轮待发布数量为0；未为验证而新建或重发通知。网页健康检查及数据库检查正常，数据库版本仍为 `20260909_0025`。

Notion原有短文案规则说明已存在，本次尝试补记生效时间时浏览器连接连续超时，未完成远端修改。以本说明记录的本地实际部署状态为准，未宣称Notion生效时间已同步。

本次不执行 Alembic 升级，两张后续安全账本仍未部署。这是文案发布，不代表开发分支的其他安全改动已上线或实单资金流程已重新验收。
